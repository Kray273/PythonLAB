# 01_ calc_tester1

import my_package.calculator # import를 통해 사용권한을 가진다.

print(my_package.calculator.plus(10,5))  #패키지.모듈.함수 순으로 호출하면 함수를 사용 가능
print(my_package.calculator.minus(10,5))
print(my_package.calculator.multiply(10,5))
print(my_package.calculator.divide(10,5))
