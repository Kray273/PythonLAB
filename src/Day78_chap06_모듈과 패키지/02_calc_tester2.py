# 02_ calc_tester2

#from my_package.calculator import * # from을 사용해 패키지와 모듈을 입력하고, import뒤에 사용하고 싶은 함수를 사용(*은 모든 것!)
from my_package.calculator import plus, minus # ','를 통해 원하는 함수의 권한을 얻을 수 있음.

print(plus(3,5)) #바로 함수를 입력해서 사용.
print(minus(3,5))
